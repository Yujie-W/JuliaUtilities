name: TagBot
on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch:

jobs:
  TagBot:
    runs-on: ubuntu-20.04
    steps:
      - name: Tagbot
        uses: JuliaRegistries/TagBot@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ssh: ${{ secrets.SSH_KEY }}
